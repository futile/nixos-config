diff --git a/engine.test.ts b/engine.test.ts
index 143501e..5bf00fb 100644
--- a/engine.test.ts
+++ b/engine.test.ts
@@ -303,0 +304 @@ test("route to a pending agent buffers; attach flushes to the real handle (R1)",
+		thinkingLevel: "xhigh",
@@ -308,0 +310 @@ test("route to a pending agent buffers; attach flushes to the real handle (R1)",
+	assert.equal(e.get("a")?.thinkingLevel, "xhigh");
diff --git a/engine.ts b/engine.ts
index 9635782..7b7b28a 100644
--- a/engine.ts
+++ b/engine.ts
@@ -45,0 +46,2 @@ export interface AgentRecord {
+	/** Effective Pi thinking level after model-capability clamping. */
+	thinkingLevel?: string;
@@ -213,0 +216 @@ export class Engine {
+			thinkingLevel?: string;
@@ -216 +219 @@ export class Engine {
-			dispose?: () => void;
+			dispose?: () => void | Promise<void>;
@@ -223,0 +227 @@ export class Engine {
+		rec.thinkingLevel = opts.thinkingLevel;
diff --git a/feed.test.ts b/feed.test.ts
index 6a65688..93a8a0e 100644
--- a/feed.test.ts
+++ b/feed.test.ts
@@ -37,0 +38,5 @@ test("formatSnapshot lists each agent with status and turns", () => {
+test("formatSnapshot shows the effective thinking level beside the model", () => {
+	const out = formatSnapshot([rec({ name: "scout", model: "openai-codex/gpt-5.6-luna", thinkingLevel: "xhigh" })], 0, 100, "main");
+	assert.match(out, /openai-codex\/gpt-5\.6-luna@xhigh/);
+});
+
diff --git a/feed.ts b/feed.ts
index bdbf842..4c1ee43 100644
--- a/feed.ts
+++ b/feed.ts
@@ -48,0 +49 @@ export function formatSnapshot(
+		const route = a.thinkingLevel ? `${a.model}@${a.thinkingLevel}` : a.model;
@@ -52 +53 @@ export function formatSnapshot(
-			`${a.model}  (by ${a.spawnedBy}${queued})`
+			`${route}  (by ${a.spawnedBy}${queued})`
diff --git a/index.ts b/index.ts
index 9398769..0e250e6 100644
--- a/index.ts
+++ b/index.ts
@@ -39 +39 @@ import { createSubagentsPanel } from "./panel.ts";
-import { danglingToolResultIds, deriveStatus, type RawMessage } from "./persistence-logic.ts";
+import { danglingToolResultIds, deriveStatus, type RawMessage, sessionSpecFromRoster } from "./persistence-logic.ts";
@@ -41 +41 @@ import { readRoster, subagentsDir, writeRoster } from "./persistence.ts";
-import { createSpawner, type ResolvedModel, type SessionLike } from "./spawner.ts";
+import { createSpawner, type ResolvedModel, type SessionLike, type ThinkingLevel } from "./spawner.ts";
@@ -67 +67,2 @@ const BUDGET_ESCALATION = (total: number) =>
-const ENGINE_KEY = "__subagentsEngine_v11";
+// v12: AgentRecord gains effective thinkingLevel; attach() sets it.
+const ENGINE_KEY = "__subagentsEngine_v12";
@@ -112 +113 @@ function agentSystemPrompt(name: string, systemPrompt: string, spawnedBy: string
-		"- spawn_agent({name, systemPrompt, overrideModel?, message?}): create a new agent (message = optional first task).",
+		"- spawn_agent({name, systemPrompt, overrideModel?, overrideThinkingLevel?, message?}): create a new agent (message = optional first task).",
@@ -273,0 +275,4 @@ export default function subagents(pi: ExtensionAPI) {
+	const captureForegroundThinking = (level: ThinkingLevel | undefined) => {
+		const u = engine.get("main");
+		if (u && level) u.thinkingLevel = level;
+	};
@@ -301,0 +307 @@ export default function subagents(pi: ExtensionAPI) {
+					thinkingLevel: a.thinkingLevel,
@@ -373 +379,2 @@ export default function subagents(pi: ExtensionAPI) {
-				"corrections) to unblock its work." +
+				"corrections) to unblock its work. Follow the active AGENTS.md model-routing policy when choosing model and " +
+				"thinking level; inheritance is only the fallback when no policy selects another route." +
@@ -384,2 +391,3 @@ export default function subagents(pi: ExtensionAPI) {
-							"Omit to inherit your current model (the default — almost always correct). Set 'provider/id' " +
-							"ONLY when the user explicitly named a specific model; a wrong value returns the available list.",
+							"Omit to inherit when no active routing policy selects another model. Otherwise pass the " +
+							"'provider/id' selected by AGENTS.md or explicitly requested by the user; a wrong value " +
+							"returns the available list.",
@@ -387,0 +396,18 @@ export default function subagents(pi: ExtensionAPI) {
+				overrideThinkingLevel: Type.Optional(
+					Type.Union(
+						[
+							Type.Literal("off"),
+							Type.Literal("minimal"),
+							Type.Literal("low"),
+							Type.Literal("medium"),
+							Type.Literal("high"),
+							Type.Literal("xhigh"),
+							Type.Literal("max"),
+						],
+						{
+							description:
+								"Omit to inherit the spawning agent's effective level. Otherwise set the level selected " +
+								"by AGENTS.md or the user; Pi clamps it to the target model's capabilities.",
+						},
+					),
+				),
@@ -395 +421,7 @@ export default function subagents(pi: ExtensionAPI) {
-					{ name: args.name, systemPrompt: args.systemPrompt, model: args.overrideModel, message: args.message },
+					{
+						name: args.name,
+						systemPrompt: args.systemPrompt,
+						model: args.overrideModel,
+						thinkingLevel: args.overrideThinkingLevel,
+						message: args.message,
+					},
@@ -544 +576,7 @@ export default function subagents(pi: ExtensionAPI) {
-		spec: { name: string; systemPrompt: string; spawnedBy: string; model: unknown },
+		spec: {
+			name: string;
+			systemPrompt: string;
+			spawnedBy: string;
+			model: unknown;
+			thinkingLevel?: ThinkingLevel;
+		},
@@ -592,0 +631 @@ export default function subagents(pi: ExtensionAPI) {
+			thinkingLevel: spec.thinkingLevel,
@@ -678 +717 @@ export default function subagents(pi: ExtensionAPI) {
-					{ name: entry.name, systemPrompt: entry.systemPrompt, spawnedBy: entry.spawnedBy, model: resolved.model },
+					sessionSpecFromRoster(entry, resolved.model),
@@ -717,0 +757,4 @@ export default function subagents(pi: ExtensionAPI) {
+	pi.on("thinking_level_select", (event) => {
+		captureForegroundThinking(event.level);
+		updateStatus();
+	});
@@ -722,0 +766 @@ export default function subagents(pi: ExtensionAPI) {
+		captureForegroundThinking(ctx.thinkingLevel);
@@ -759,0 +804 @@ export default function subagents(pi: ExtensionAPI) {
+		captureForegroundThinking(ctx.thinkingLevel);
@@ -779,0 +825 @@ export default function subagents(pi: ExtensionAPI) {
+				thinkingLevel: ctx.thinkingLevel,
diff --git a/panel-logic.test.ts b/panel-logic.test.ts
index ec91c98..a365498 100644
--- a/panel-logic.test.ts
+++ b/panel-logic.test.ts
@@ -12,0 +13 @@ import {
+	formatModel,
@@ -182,3 +183,6 @@ test("formatRoster: passes the error tone to the styler", () => {
-test("formatRoster: model id shown in full (provider prefix dropped, no hard cap)", () => {
-	const row = formatRoster([re({ model: "anthropic/claude-sonnet-4-5", status: "thinking" })], 200)[0];
-	assert.match(row, /claude-sonnet-4-5/);
+test("formatRoster: model id and effective thinking level are shown together", () => {
+	const row = formatRoster(
+		[re({ model: "openai-codex/gpt-5.6-luna", thinkingLevel: "xhigh", status: "thinking" })],
+		200,
+	)[0];
+	assert.match(row, /gpt-5\.6-luna@xhigh/);
@@ -265,0 +270,2 @@ test("shortModel drops the provider prefix, no truncation (model column sizes it
+	assert.equal(formatModel("openai-codex/gpt-5.6-luna", "xhigh"), "gpt-5.6-luna@xhigh");
+	assert.equal(formatModel("anthropic/opus", undefined), "opus");
diff --git a/panel-logic.ts b/panel-logic.ts
index 17074e4..e3ef2ef 100644
--- a/panel-logic.ts
+++ b/panel-logic.ts
@@ -93,0 +94 @@ export interface RosterEntry {
+	thinkingLevel?: string;
@@ -143,0 +145,6 @@ export function shortModel(model: string | undefined): string {
+/** Compact model + effective thinking level for the headerless roster. */
+export function formatModel(model: string | undefined, thinkingLevel: string | undefined): string {
+	const short = shortModel(model);
+	return thinkingLevel ? `${short}@${thinkingLevel}` : short;
+}
+
@@ -210 +217 @@ export function formatRoster(
-		{ key: "model", collapseRank: 1, fit: "end", cellOf: (e) => shortModel(e.model) },
+		{ key: "model", collapseRank: 1, fit: "end", cellOf: (e) => formatModel(e.model, e.thinkingLevel) },
diff --git a/panel.ts b/panel.ts
index a7b1de0..c848614 100644
--- a/panel.ts
+++ b/panel.ts
@@ -283,0 +284 @@ export function createSubagentsPanel(deps: PanelDeps, tui: TuiLike, theme: Theme
+					thinkingLevel: a.thinkingLevel,
diff --git a/persistence-logic.test.ts b/persistence-logic.test.ts
index 48a1d55..3b47eaa 100644
--- a/persistence-logic.test.ts
+++ b/persistence-logic.test.ts
@@ -8,0 +9 @@ import {
+	sessionSpecFromRoster,
@@ -70 +71,9 @@ test("roster round-trip serialize -> parse", () => {
-		{ name: "a", spawnedBy: "main", depth: 1, model: "p/q", systemPrompt: "do x", sessionFile: "/a.jsonl" },
+		{
+			name: "a",
+			spawnedBy: "main",
+			depth: 1,
+			model: "p/q",
+			thinkingLevel: "xhigh",
+			systemPrompt: "do x",
+			sessionFile: "/a.jsonl",
+		},
@@ -75,0 +85 @@ test("roster round-trip serialize -> parse", () => {
+	assert.equal(roster[0].thinkingLevel, "xhigh");
@@ -79,0 +90,36 @@ test("roster round-trip serialize -> parse", () => {
+test("empty-session restore forwards the roster thinking level without transcript evidence", () => {
+	const [entry] = parseRoster(
+		JSON.stringify([
+			{
+				name: "empty",
+				spawnedBy: "main",
+				depth: 1,
+				model: "p/q",
+				thinkingLevel: "xhigh",
+				systemPrompt: "wait",
+				sessionFile: "/empty.jsonl",
+			},
+		]),
+	);
+	assert.deepEqual(sessionSpecFromRoster(entry, { id: "resolved" }), {
+		name: "empty",
+		systemPrompt: "wait",
+		spawnedBy: "main",
+		model: { id: "resolved" },
+		thinkingLevel: "xhigh",
+	});
+});
+
+test("parseRoster: accepts entries without optional thinking level and rejects invalid levels", () => {
+	const withoutThinking = {
+		name: "a",
+		spawnedBy: "main",
+		depth: 1,
+		model: "p/q",
+		systemPrompt: "do x",
+		sessionFile: "/a.jsonl",
+	};
+	assert.deepEqual(parseRoster(JSON.stringify([withoutThinking])), [withoutThinking]);
+	assert.deepEqual(parseRoster(JSON.stringify([{ ...withoutThinking, thinkingLevel: "ultra" }])), []);
+});
+
diff --git a/persistence-logic.ts b/persistence-logic.ts
index 9e81665..8822d66 100644
--- a/persistence-logic.ts
+++ b/persistence-logic.ts
@@ -77,0 +78,5 @@ export function deriveStatus(messages: RawMessage[]): "idle" | "halted" {
+export type PersistedThinkingLevel = "off" | "minimal" | "low" | "medium" | "high" | "xhigh" | "max";
+
+const isThinkingLevel = (value: unknown): value is PersistedThinkingLevel =>
+	typeof value === "string" && ["off", "minimal", "low", "medium", "high", "xhigh", "max"].includes(value);
+
@@ -82,0 +88 @@ export interface RosterEntry {
+	thinkingLevel?: PersistedThinkingLevel;
@@ -86,0 +93,11 @@ export interface RosterEntry {
+/** Build the SDK session inputs that remain authoritative even when the JSONL has no messages. */
+export function sessionSpecFromRoster<T>(entry: RosterEntry, model: T) {
+	return {
+		name: entry.name,
+		systemPrompt: entry.systemPrompt,
+		spawnedBy: entry.spawnedBy,
+		model,
+		thinkingLevel: entry.thinkingLevel,
+	};
+}
+
@@ -93,0 +111 @@ export function serializeRoster(
+		thinkingLevel?: string;
@@ -104,0 +123 @@ export function serializeRoster(
+			...(isThinkingLevel(a.thinkingLevel) ? { thinkingLevel: a.thinkingLevel } : {}),
@@ -125,0 +145 @@ export function parseRoster(json: string): RosterEntry[] {
+			(e.thinkingLevel === undefined || isThinkingLevel(e.thinkingLevel)) &&
diff --git a/spawner.test.ts b/spawner.test.ts
index 2850928..2800161 100644
--- a/spawner.test.ts
+++ b/spawner.test.ts
@@ -4 +4 @@ import { Engine } from "./engine.ts";
-import { createSpawner, type SessionLike } from "./spawner.ts";
+import { createSpawner, type SessionLike, type ThinkingLevel } from "./spawner.ts";
@@ -13,0 +14 @@ class FakeSession implements SessionLike {
+	thinkingLevel: ThinkingLevel = "high";
@@ -42,0 +44 @@ function withMain(engine: Engine, inbox: string[]) {
+		thinkingLevel: "xhigh",
@@ -105,0 +108,69 @@ test("smoke: spawn -> deliver -> reply -> budget abort -> halt", async () => {
+test("spawn passes an explicit thinking override and records the effective clamped level", async () => {
+	const engine = new Engine({ maxAgents: 8, maxSpawnDepth: 3, turnBudget: 5 });
+	withMain(engine, []);
+	let requested: ThinkingLevel | undefined;
+	const spawner = createSpawner({
+		engine,
+		resolveModel: () => ({ provider: "openai-codex", id: "gpt-5.6-luna", model: {} }),
+		createSession: async (spec) => {
+			requested = spec.thinkingLevel;
+			const session = new FakeSession();
+			session.thinkingLevel = "xhigh"; // SDK's effective value after capability clamping
+			return { session };
+		},
+	});
+
+	const result = await spawner.spawnAgent(
+		{ name: "scout", systemPrompt: "inspect", model: "openai-codex/gpt-5.6-luna", thinkingLevel: "max" },
+		"main",
+	);
+	assert.equal(requested, "max");
+	assert.equal(engine.get("scout")?.thinkingLevel, "xhigh");
+	assert.match(result.msg, /gpt-5\.6-luna, thinking xhigh/);
+});
+
+test("nested children inherit their parent's effective thinking level", async () => {
+	const engine = new Engine({ maxAgents: 8, maxSpawnDepth: 3, turnBudget: 5 });
+	withMain(engine, []);
+	const requested: Array<ThinkingLevel | undefined> = [];
+	const spawner = createSpawner({
+		engine,
+		resolveModel: () => ({ provider: "test", id: "m", model: {} }),
+		createSession: async (spec) => {
+			requested.push(spec.thinkingLevel);
+			const session = new FakeSession();
+			session.thinkingLevel = spec.thinkingLevel ?? "medium";
+			return { session };
+		},
+	});
+
+	await spawner.spawnAgent({ name: "lead", systemPrompt: "lead" }, "main");
+	await spawner.spawnAgent({ name: "worker", systemPrompt: "work" }, "lead");
+	assert.deepEqual(requested, ["xhigh", "xhigh"]);
+	assert.equal(engine.get("worker")?.thinkingLevel, "xhigh");
+});
+
+test("restored agents expose the persisted session's effective thinking level", () => {
+	const engine = new Engine({ maxAgents: 8, maxSpawnDepth: 3, turnBudget: 5 });
+	withMain(engine, []);
+	const session = new FakeSession();
+	session.thinkingLevel = "high";
+	const spawner = createSpawner({
+		engine,
+		resolveModel: () => ({ provider: "test", id: "m", model: {} }),
+		createSession: async () => ({ session: new FakeSession() }),
+	});
+
+	spawner.restoreAgent({
+		name: "restored",
+		spawnedBy: "main",
+		depth: 1,
+		model: "test/m",
+		systemPrompt: "resume",
+		sessionFile: "/tmp/restored.jsonl",
+		session,
+		halted: false,
+	});
+	assert.equal(engine.get("restored")?.thinkingLevel, "high");
+});
+
diff --git a/spawner.ts b/spawner.ts
index 267b79f..957ac21 100644
--- a/spawner.ts
+++ b/spawner.ts
@@ -7,0 +8,3 @@ import type { AgentHandle, AgentView, Engine, StopReason } from "./engine.ts";
+/** Pi thinking levels, redeclared so this orchestration layer remains SDK-free. */
+export type ThinkingLevel = "off" | "minimal" | "low" | "medium" | "high" | "xhigh" | "max";
+
@@ -22,0 +26,2 @@ export interface SessionLike {
+	/** Effective level after the SDK clamps the requested level to model capabilities. */
+	readonly thinkingLevel: ThinkingLevel;
@@ -39,0 +45,2 @@ export interface SpawnSpec {
+	/** Optional override; omitted means inherit the spawning agent's effective level. */
+	thinkingLevel?: ThinkingLevel;
@@ -60,0 +68 @@ export interface SpawnerDeps {
+		thinkingLevel?: ThinkingLevel;
@@ -175,0 +184 @@ export function createSpawner(deps: SpawnerDeps): Spawner {
+			thinkingLevel: spec.session.thinkingLevel,
@@ -192 +201,3 @@ export function createSpawner(deps: SpawnerDeps): Spawner {
-		const inheritRef = spec.model ?? engine.get(spawnerName)?.model;
+		const spawner = engine.get(spawnerName);
+		const inheritRef = spec.model ?? spawner?.model;
+		const thinkingLevel = spec.thinkingLevel ?? (spawner?.thinkingLevel as ThinkingLevel | undefined);
@@ -214,0 +226 @@ export function createSpawner(deps: SpawnerDeps): Spawner {
+				thinkingLevel,
@@ -224,0 +237 @@ export function createSpawner(deps: SpawnerDeps): Spawner {
+			thinkingLevel: session.thinkingLevel,
@@ -240 +253,4 @@ export function createSpawner(deps: SpawnerDeps): Spawner {
-		return { ok: true, msg: `spawned '${spec.name}' (model ${resolved.provider}/${resolved.id})${sent}` };
+		return {
+			ok: true,
+			msg: `spawned '${spec.name}' (model ${resolved.provider}/${resolved.id}, thinking ${session.thinkingLevel})${sent}`,
+		};
